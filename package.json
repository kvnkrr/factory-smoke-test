{
  "name": "product",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"No tests yet — factory-agent will generate tests\" && exit 0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
