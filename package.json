{
  "name": "factory-smoke-test",
  "version": "1.0.0",
  "private": true,
  "description": "Single-page Factory Smoke Test production line visualisation (no build, no deps).",
  "license": "UNLICENSED",
  "scripts": {
    "start": "npx serve .",
    "test": "echo OK"
  }
}
