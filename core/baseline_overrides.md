# Baseline Overrides

This file documents human-approved modifications to legacy test files.
Each entry must include: Date, Run ID, Justification, and Files modified.

Immutable test baselines are the default rule. Only add entries here when
a legacy test must be corrected due to genuinely incorrect original behavior.

<!-- Example entry:

## 2026-03-03 — Run 004

**Justification:**
Original v1 test encoded incorrect behavior regarding authentication timeout.

**Changes:**
- tests/auth.v1.test.js updated

-->
